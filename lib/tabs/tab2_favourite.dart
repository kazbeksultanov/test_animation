import 'package:flutter/material.dart';

class Tab2Favourite extends StatelessWidget {
  const Tab2Favourite({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text(runtimeType.toString()),
    );
  }
}
