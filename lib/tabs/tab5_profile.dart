import 'package:flutter/material.dart';

class Tab5Profile extends StatelessWidget {
  const Tab5Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text(runtimeType.toString()),
    );
  }
}
